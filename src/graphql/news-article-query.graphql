query NewsArticle($slug: String) {
  newsArticle(filter: { slug: { eq: $slug } }) {
    seo: _seoMetaTags {
      attributes
      content
      tag
    }
    __typename
    id
    slug
    title
    sections {
      __typename
      ...TeaserRelatedNews
    }
  }
}
