import { MediaProperties } from "@/components/media/interfaces";

export interface HeaderCaseStudyProperties {
  title: string;
  subtitle: string;
  featuredMedia: MediaProperties;
  backgroundColor: string;
  inverted: boolean;
}
