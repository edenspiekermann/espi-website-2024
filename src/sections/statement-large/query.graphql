fragment StatementLarge on StatementLargeRecord {
  __typename
  id
  text(markdown: true)
  backgroundColor
  addCallToAction
  cta {
    __typename
    ... on PageLinkRecord {
      text
      slug
    }
  }
}
